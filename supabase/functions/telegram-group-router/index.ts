import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "https://esm.sh/@supabase/supabase-js@2.55.0";
const BASE_URL=Deno.env.get('SUPABASE_URL')||'';const KEY=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'';const TOKEN=Deno.env.get('TELEGRAM_BOT_TOKEN')||'';const sb=createClient(BASE_URL,KEY,{auth:{persistSession:false,autoRefreshToken:false}});const json=(d:unknown,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json'}});function same(a:string,b:string){if(!a||a.length!==b.length)return false;let m=0;for(let i=0;i<a.length;i++)m|=a.charCodeAt(i)^b.charCodeAt(i);return m===0}async function secret(){const{data,error}=await sb.rpc('get_bot_internal_secret',{p_name:'adsreward_telegram_webhook_secret'});if(error||!data)throw error||new Error('Missing webhook secret');return String(data)}async function tg(method:string,payload:any){const r=await fetch(`https://api.telegram.org/bot${TOKEN}/${method}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const b=await r.json().catch(()=>({ok:false}));if(!r.ok||!b.ok)throw new Error(b.description||method);return b.result}
Deno.serve(async(req:Request)=>{try{if(req.method==='GET'){if(new URL(req.url).searchParams.get('sync')==='1'){const wh=await secret();await tg('setWebhook',{url:`${BASE_URL}/functions/v1/telegram-group-router`,secret_token:wh,allowed_updates:['message','callback_query','channel_post','my_chat_member'],drop_pending_updates:false});return json({success:true,synced:true})}return json({success:true,service:'telegram-group-router'})}if(req.method!=='POST')return json({success:false},405);const wh=await secret();const got=req.headers.get('x-telegram-bot-api-secret-token')||'';if(!same(wh,got))return json({success:false,error:'unauthorized'},401);const body=await req.json().catch(()=>({}));const h={'Content-Type':'application/json','x-telegram-bot-api-secret-token':wh};try{const gr=await fetch(`${BASE_URL}/functions/v1/telegram-group-rewards`,{method:'POST',headers:h,body:JSON.stringify(body)});const gj=await gr.json().catch(()=>({handled:false}));if(gj?.handled)return json({success:true,handledBy:'telegram-group-rewards'})}catch(e){console.error('group rewards route failed',e)}const r=await fetch(`${BASE_URL}/functions/v1/telegram-router`,{method:'POST',headers:h,body:JSON.stringify(body)});const t=await r.text();if(!r.ok)console.error('legacy router failed',r.status,t);return json({success:true,forwarded:r.ok})}catch(e){console.error('telegram-group-router',e);return json({success:false,error:(e as Error).message},500)}});