import React,{useCallback,useEffect,useMemo,useState} from 'react';
import {BadgeDollarSign,Clock3,Gift,Leaf,LockKeyhole,PlayCircle,RefreshCw,ShieldCheck,Sparkles,TimerReset,WalletCards} from 'lucide-react';
import {useApp} from '@/context/AppContext';
import {usePreferences} from '@/context/PreferencesContext';
import {claimDailyDrop,claimFarm,getHomeRewardState,getTodayAdCount,getTransactions,logAdWatch,startFarm,type HomeRewardState} from '@/lib/api';
import {showRewardAd,type RewardAdProvider} from '@/lib/adNetworks';

const CSS=`
.hv-root{padding:2px 15px 116px;color:inherit}.hv-hero,.hv-card,.hv-provider,.hv-tx{position:relative;overflow:hidden;border:1px solid rgba(255,255,255,.1);background:linear-gradient(145deg,rgba(255,255,255,.095),rgba(255,255,255,.025)),rgba(8,13,24,.46);box-shadow:inset 0 1px rgba(255,255,255,.14),0 14px 36px rgba(0,0,0,.18);backdrop-filter:blur(24px) saturate(145%)}.hv-hero{border-radius:26px;padding:18px;margin-bottom:11px}.hv-kicker{font:700 9px 'Orbitron',sans-serif;letter-spacing:2.4px;opacity:.38;text-transform:uppercase;display:flex;align-items:center;gap:6px}.hv-kicker svg{width:13px}.hv-balance{font:900 34px 'Orbitron',sans-serif;margin:6px 0 2px;color:#ffe08a}.hv-hero-sub{font-size:11px;opacity:.44}.hv-grid{display:grid;grid-template-columns:1fr 1fr;gap:9px;margin-bottom:10px}.hv-card{border-radius:22px;padding:15px;min-height:168px}.hv-card.full{grid-column:1/-1;min-height:auto}.hv-top{display:flex;align-items:center;justify-content:space-between;gap:8px}.hv-icon{width:39px;height:39px;border-radius:14px;display:grid;place-items:center;border:1px solid rgba(255,255,255,.1);background:rgba(255,255,255,.055)}.hv-icon svg{width:19px;height:19px}.hv-title{font:800 11px 'Orbitron',sans-serif;letter-spacing:.8px;margin-top:12px}.hv-sub{font-size:11px;line-height:1.38;opacity:.47;margin-top:5px}.hv-value{font:800 17px 'Orbitron',sans-serif;margin-top:10px}.hv-bar{height:6px;border-radius:99px;background:rgba(255,255,255,.065);overflow:hidden;margin:10px 0}.hv-fill{height:100%;border-radius:99px;background:linear-gradient(90deg,#60a5fa,#34d399);transition:width .35s}.hv-btn{width:100%;border:0;border-radius:14px;padding:11px 10px;font:750 10px 'Orbitron',sans-serif;letter-spacing:.65px;display:flex;align-items:center;justify-content:center;gap:7px;background:linear-gradient(135deg,#ffd84d,#f59e0b);color:#171006}.hv-btn svg{width:15px}.hv-btn.secondary{background:rgba(255,255,255,.055);border:1px solid rgba(255,255,255,.09);color:inherit}.hv-btn:disabled{opacity:.42}.hv-days{display:flex;gap:5px;overflow:auto;margin:12px 0 10px;scrollbar-width:none}.hv-days::-webkit-scrollbar{display:none}.hv-day{min-width:43px;flex:1;padding:8px 3px;border-radius:12px;text-align:center;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.035)}.hv-day.done{border-color:rgba(52,211,153,.28);background:rgba(52,211,153,.08)}.hv-day.now{border-color:rgba(255,208,80,.38)}.hv-day-pts{font:800 12px 'Orbitron',sans-serif}.hv-day-label{font-size:8px;opacity:.38;margin-top:3px}.hv-networks{display:grid;gap:8px}.hv-provider{border-radius:18px;padding:12px;display:flex;align-items:center;gap:11px}.hv-provider-icon{width:42px;height:42px;border-radius:14px;background:rgba(255,255,255,.06);display:grid;place-items:center;flex:0 0 auto}.hv-provider-icon svg{width:19px}.hv-provider-body{flex:1;min-width:0}.hv-provider-name{font-weight:800;font-size:13px}.hv-provider-sub{font-size:10px;opacity:.43;margin-top:2px}.hv-provider-btn{width:42px;height:42px;border-radius:14px;border:1px solid rgba(255,210,90,.24);background:rgba(255,190,0,.09);display:grid;place-items:center;color:#ffd45c}.hv-provider-btn svg{width:18px}.hv-provider-btn:disabled{opacity:.35}.hv-section{font:700 9px 'Orbitron',sans-serif;letter-spacing:2px;opacity:.32;text-transform:uppercase;margin:16px 2px 8px}.hv-message{padding:10px 12px;border-radius:14px;margin-bottom:10px;font-size:11px;background:rgba(34,211,238,.07);border:1px solid rgba(34,211,238,.14)}.hv-tx{border-radius:15px;padding:11px 12px;margin-bottom:7px;display:flex;align-items:center;gap:10px}.hv-tx svg{width:17px;opacity:.7}.hv-tx-main{flex:1}.hv-tx-title{font-size:12px;font-weight:700;text-transform:capitalize}.hv-tx-points{font:700 11px 'Orbitron',sans-serif;color:#ffd45c}.hv-loading{padding:34px 10px;text-align:center;opacity:.45;font:700 9px 'Orbitron',sans-serif;letter-spacing:2px}.hv-spin{animation:hvspin 1s linear infinite}@keyframes hvspin{to{transform:rotate(360deg)}}
[data-theme='light'] .hv-hero,[data-theme='light'] .hv-card,[data-theme='light'] .hv-provider,[data-theme='light'] .hv-tx{background:linear-gradient(145deg,rgba(255,255,255,.96),rgba(245,248,255,.8));border-color:rgba(15,23,42,.09);box-shadow:0 12px 32px rgba(45,58,85,.08);color:#0f172a}[data-theme='light'] .hv-icon,[data-theme='light'] .hv-provider-icon,[data-theme='light'] .hv-btn.secondary,[data-theme='light'] .hv-day{background:rgba(15,23,42,.035);border-color:rgba(15,23,42,.08);color:#0f172a}[data-theme='light'] .hv-bar{background:rgba(15,23,42,.07)}[data-theme='light'] .hv-balance{color:#9a6300}
`;

const fmt=(ms:number)=>{const total=Math.max(0,Math.floor(ms/1000));const h=Math.floor(total/3600),m=Math.floor((total%3600)/60),s=total%60;return `${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`};

export default function HomePage(){
 const {balance,settings,refreshBalance}=useApp();const{t}=usePreferences();
 const [home,setHome]=useState<HomeRewardState|null>(null);const[adsToday,setAdsToday]=useState<number|null>(null);const[transactions,setTransactions]=useState<any[]>([]);const[busy,setBusy]=useState<string|null>(null);const[message,setMessage]=useState('');const[now,setNow]=useState(Date.now());
 const ready=Object.keys(settings).length>0&&home!==null&&adsToday!==null;
 const adReward=Number(settings.ad_reward_points);const adLimit=Number(settings.max_ads_per_day);const adCooldown=Number(settings.ad_cooldown_seconds);const balancePoints=balance?.points;
 const load=useCallback(async()=>{const[state,count,tx]=await Promise.all([getHomeRewardState(),getTodayAdCount(),getTransactions('self')]);if(state)setHome(state);setAdsToday(count);setTransactions((tx||[]).slice(0,5))},[]);
 useEffect(()=>{void load();const id=setInterval(()=>setNow(Date.now()),1000);const refresh=()=>void load();window.addEventListener('focus',refresh);return()=>{clearInterval(id);window.removeEventListener('focus',refresh)}},[load]);
 useEffect(()=>{if(home?.drop?.nextResetAt&&new Date(home.drop.nextResetAt).getTime()<=now)void load()},[now,home?.drop?.nextResetAt,load]);
 const flash=(m:string)=>{setMessage(m);setTimeout(()=>setMessage(''),2600)};
 const farmReadyAt=home?.farm.readyAt?new Date(home.farm.readyAt).getTime():0;const farmStarted=!!home?.farm.startedAt;const farmReady=farmStarted&&farmReadyAt<=now;const farmPct=farmStarted?Math.min(100,Math.max(0,100-(Math.max(0,farmReadyAt-now)/(Math.max(1,home!.farm.durationMinutes)*60000))*100)):0;
 const nextDropMs=home?.drop.nextResetAt?new Date(home.drop.nextResetAt).getTime()-now:0;const currentDropDay=Math.max(1,Math.min(home?.drop.maxDays||1,(home?.drop.streak||0)+(home?.drop.claimedToday?0:1)));
 const dropDays=useMemo(()=>home?Array.from({length:home.drop.maxDays},(_,i)=>({day:i+1,points:home.drop.base+i*home.drop.increment})):[],[home]);
 async function farmAction(){if(!home)return;setBusy('farm');const r=farmReady?await claimFarm():await startFarm();if(r.success){flash(farmReady?`+${r.points||home.farm.rewardPoints} ${t('points')}`:t('farmStarted'));await refreshBalance();await load()}else flash(r.message||t('tryAgain'));setBusy(null)}
 async function dropAction(){if(!home||home.drop.claimedToday)return;setBusy('drop');const r=await claimDailyDrop();if(r.success){flash(`+${r.points||0} ${t('points')}`);await refreshBalance();await load()}else flash(r.message||t('tryAgain'));setBusy(null)}
 async function watch(provider:RewardAdProvider){if(!ready||busy||adsToday!>=adLimit)return;setBusy(provider);try{await showRewardAd(provider);const r:any=await logAdWatch('self','ad_watch',adReward,provider);if(!r.success)throw new Error(r.message||t('adRewardFailed'));flash(`+${r.points??adReward} ${t('points')}`);await refreshBalance();await load()}catch(e){flash((e as Error).message)}finally{setBusy(null)}}
 const providers:[RewardAdProvider,string,React.ComponentType<any>][]=[['adsgram','Adsgram',PlayCircle],['monetag','Monetag',BadgeDollarSign],['gigapub','GigaPub',ShieldCheck]];
 return <><style>{CSS}</style><div className="hv-root">
  {!ready?<div className="hv-loading"><RefreshCw className="hv-spin" style={{width:20,margin:'0 auto 10px'}}/>{t('loading')}</div>:<>
   {message&&<div className="hv-message">{message}</div>}
   <section className="hv-hero"><div className="hv-kicker"><WalletCards/>{t('availableBalance')}</div><div className="hv-balance">{balancePoints==null?'•••':balancePoints.toLocaleString()}</div><div className="hv-hero-sub">{t('homeBalanceSub')}</div></section>
   <div className="hv-grid">
    <section className="hv-card"><div className="hv-top"><div className="hv-icon"><Leaf/></div><span className="hv-kicker">+{home!.farm.rewardPoints}</span></div><div className="hv-title">{t('farming')}</div><div className="hv-sub">{farmStarted?(farmReady?t('farmReady'):`${t('readyIn')} ${fmt(farmReadyAt-now)}`):`${home!.farm.durationMinutes} ${t('minutes')} · +${home!.farm.rewardPoints} ${t('points')}`}</div><div className="hv-bar"><div className="hv-fill" style={{width:`${farmPct}%`}}/></div><button className={`hv-btn ${farmStarted&&!farmReady?'secondary':''}`} disabled={busy==='farm'||(farmStarted&&!farmReady)} onClick={farmAction}>{farmStarted&&!farmReady?<><Clock3/>{fmt(farmReadyAt-now)}</>:farmReady?<><Sparkles/>{t('collectReward')}</>:<><PlayCircle/>{t('startFarming')}</>}</button></section>
    <section className="hv-card"><div className="hv-top"><div className="hv-icon"><Gift/></div><span className="hv-kicker">{t('day')} {currentDropDay}</span></div><div className="hv-title">{t('dailyDrop')}</div><div className="hv-value">+{dropDays[currentDropDay-1]?.points||0}</div><div className="hv-sub">{home!.drop.claimedToday?t('dropLockedUntilReset'):t('dailyDropAvailable')}</div><button className={`hv-btn ${home!.drop.claimedToday?'secondary':''}`} disabled={home!.drop.claimedToday||busy==='drop'} onClick={dropAction}>{home!.drop.claimedToday?<><LockKeyhole/>{fmt(nextDropMs)}</>:<><Gift/>{t('collectDailyDrop')}</>}</button></section>
    <section className="hv-card full"><div className="hv-top"><div><div className="hv-kicker"><TimerReset/>{t('dailyDrop')}</div><div className="hv-title">{t('streakProgress')}</div></div><span className="hv-kicker">{home!.drop.streak}/{home!.drop.maxDays}</span></div><div className="hv-days">{dropDays.map((d,i)=>{const done=i<(home!.drop.claimedToday?home!.drop.streak:Math.max(0,home!.drop.streak));const isNow=i===currentDropDay-1&&!home!.drop.claimedToday;return <div key={d.day} className={`hv-day ${done?'done':''} ${isNow?'now':''}`}><div className="hv-day-pts">{d.points}</div><div className="hv-day-label">D{d.day}</div></div>})}</div>{home!.drop.claimedToday&&<div className="hv-sub" style={{display:'flex',alignItems:'center',gap:6}}><LockKeyhole style={{width:14}}/>{t('nextDropIn')} {fmt(nextDropMs)}</div>}</section>
   </div>
   <div className="hv-section">{t('watchAndEarn')}</div>
   <section className="hv-card full"><div className="hv-top"><div><div className="hv-title" style={{marginTop:0}}>{t('rewardedAds')}</div><div className="hv-sub">{adsToday} / {adLimit} {t('today')} · +{adReward} {t('points')} · {adCooldown}s {t('cooldown')}</div></div><div className="hv-icon"><PlayCircle/></div></div><div className="hv-bar"><div className="hv-fill" style={{width:`${Math.min(100,(adsToday!/Math.max(1,adLimit))*100)}%`}}/></div><div className="hv-networks">{providers.map(([id,name,Icon])=><div className="hv-provider" key={id}><div className="hv-provider-icon"><Icon/></div><div className="hv-provider-body"><div className="hv-provider-name">{name}</div><div className="hv-provider-sub">+{adReward} {t('points')} · {t('rewardAfterCompletedAd')}</div></div><button className="hv-provider-btn" disabled={!!busy||adsToday!>=adLimit} onClick={()=>watch(id)} aria-label={`${t('watchAds')} ${name}`}>{busy===id?<RefreshCw className="hv-spin"/>:<PlayCircle/>}</button></div>)}</div></section>
   <div className="hv-section">{t('recentActivity')}</div>
   {transactions.length===0?<div className="hv-sub" style={{padding:'12px 2px'}}>{t('noTransactions')}</div>:transactions.map(tx=><div className="hv-tx" key={tx.id}><Sparkles/><div className="hv-tx-main"><div className="hv-tx-title">{String(tx.type||'reward').replaceAll('_',' ')}</div></div><div className="hv-tx-points">{Number(tx.points)>0?'+':''}{Number(tx.points||0)}</div></div>)}
  </>}
 </div></>;
}
