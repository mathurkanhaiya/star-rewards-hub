import React,{useEffect,useMemo,useState}from'react';
import{CalendarDays,CheckCircle2,ChevronRight,Gift,Globe2,ListChecks,LoaderCircle,PlayCircle,Send,UsersRound,XCircle}from'lucide-react';
import{useApp}from'@/context/AppContext';import{usePreferences}from'@/context/PreferencesContext';import{getTasks,getUserTasks,completeTask}from'@/lib/api';import{Task}from'@/types/telegram';
function haptic(type:'impact'|'success'|'error'='impact'){const h=(window as any).Telegram?.WebApp?.HapticFeedback;if(type==='impact')h?.impactOccurred('medium');else h?.notificationOccurred(type)}
const TYPE:Record<string,{key:string;color:string;Icon:any}>={social:{key:'social',color:'#22d3ee',Icon:Send},daily:{key:'daily',color:'#fbbf24',Icon:CalendarDays},referral:{key:'referral',color:'#4ade80',Icon:UsersRound},video:{key:'video',color:'#a855f7',Icon:PlayCircle},special:{key:'special',color:'#fb7185',Icon:Gift}};
const CSS=`.tk-root{padding:0 16px 112px;color:inherit}.tk-head{display:flex;align-items:flex-end;justify-content:space-between;margin:3px 0 15px}.tk-kicker{font:700 9px 'Orbitron',sans-serif;letter-spacing:3px;opacity:.34;text-transform:uppercase}.tk-title{font:900 23px 'Orbitron',sans-serif;margin-top:4px}.tk-head-icon{width:45px;height:45px;border-radius:15px;display:grid;place-items:center;background:rgba(255,216,77,.08);border:1px solid rgba(255,216,77,.18);color:#ffd84d}.tk-head-icon svg{width:21px}.tk-filters{display:flex;gap:7px;overflow:auto;padding:1px 0 12px;scrollbar-width:none}.tk-filters::-webkit-scrollbar{display:none}.tk-filter{flex:0 0 auto;padding:8px 12px;border-radius:13px;border:1px solid rgba(255,255,255,.08);background:rgba(255,255,255,.035);color:inherit;font-size:10px;font-weight:750}.tk-filter.active{color:#171006;background:#ffd84d;border-color:#ffd84d}.tk-summary{display:flex;justify-content:space-between;align-items:center;margin:3px 2px 9px;font-size:10px}.tk-summary span:first-child{font:700 9px 'Orbitron',sans-serif;letter-spacing:1.5px;opacity:.4;text-transform:uppercase}.tk-summary b{color:#67e8f9}.tk-list{display:grid;gap:9px}.tk-card{position:relative;overflow:hidden;border-radius:21px;padding:14px;background:linear-gradient(145deg,rgba(255,255,255,.1),rgba(255,255,255,.025)),rgba(10,15,27,.5);border:1px solid rgba(255,255,255,.09);box-shadow:inset 0 1px rgba(255,255,255,.13),0 12px 30px rgba(0,0,0,.17);backdrop-filter:blur(22px)}.tk-row{display:flex;align-items:center;gap:12px}.tk-icon{width:50px;height:50px;border-radius:15px;display:grid;place-items:center;flex:0 0 50px}.tk-icon svg{width:23px}.tk-body{min-width:0;flex:1}.tk-tag{font-size:8px;font-weight:850;letter-spacing:1.2px;text-transform:uppercase;margin-bottom:3px}.tk-name{font-size:14px;font-weight:850;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tk-desc{font-size:10px;opacity:.43;margin-top:2px;line-height:1.35;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.tk-reward{font:750 10px 'Orbitron',sans-serif;margin-top:6px}.tk-go{width:38px;height:38px;border-radius:13px;border:0;display:grid;place-items:center;color:#071016;flex:0 0 38px}.tk-go svg{width:18px}.tk-go:disabled{opacity:.45}.tk-msg{display:flex;align-items:center;gap:6px;margin-top:10px;padding:9px 10px;border-radius:12px;font-size:10px}.tk-msg svg{width:14px}.tk-msg.ok{color:#86efac;background:rgba(74,222,128,.08)}.tk-msg.err{color:#fca5a5;background:rgba(239,68,68,.08)}.tk-empty,.tk-loading{padding:38px 10px;text-align:center;border-radius:20px;border:1px dashed rgba(255,255,255,.09);opacity:.48}.tk-empty svg,.tk-loading svg{width:28px;margin:0 auto 9px}.tk-loading svg{animation:tkspin 1s linear infinite}@keyframes tkspin{to{transform:rotate(360deg)}}[data-theme='light'] .tk-card{background:rgba(255,255,255,.92);border-color:rgba(15,23,42,.08);box-shadow:0 12px 28px rgba(45,60,90,.07);color:#0f172a}[data-theme='light'] .tk-filter{background:rgba(255,255,255,.82);border-color:rgba(15,23,42,.08);color:#334155}[data-theme='light'] .tk-filter.active{background:#f6c949;color:#2b2105}[data-theme='light'] .tk-empty,[data-theme='light'] .tk-loading{border-color:rgba(15,23,42,.1);color:#475569}`;
export default function TasksPage(){const{user,refreshBalance}=useApp();const{t}=usePreferences();const[tasks,setTasks]=useState<Task[]>([]),[completing,setCompleting]=useState<string|null>(null),[message,setMessage]=useState<{id:string;text:string;success:boolean}|null>(null),[filter,setFilter]=useState('all'),[loading,setLoading]=useState(true);async function load(){if(!user)return;setLoading(true);const[all,userTasks]=await Promise.all([getTasks(),getUserTasks(user.id)]);const ids=new Set((userTasks as Array<{task_id:string}>).map(x=>x.task_id));setTasks(all.filter(x=>x.is_repeatable||!ids.has(x.id)).sort((a,b)=>b.reward_points-a.reward_points));setLoading(false)}useEffect(()=>{void load();const id=setInterval(load,30000);return()=>clearInterval(id)},[user]);async function complete(task:Task){if(!user||completing)return;haptic();setCompleting(task.id);try{if(task.link){const tg=(window as any).Telegram?.WebApp;if(tg?.openTelegramLink&&task.link.includes('t.me'))tg.openTelegramLink(task.link);else tg?.openLink?tg.openLink(task.link):window.open(task.link,'_blank')}const r=await completeTask(user.id,task.id);if(!r.success)throw new Error(r.message||t('taskFailed'));haptic('success');setMessage({id:task.id,text:`+${r.points||task.reward_points} ${t('points')}`,success:true});setTasks(p=>p.filter(x=>x.id!==task.id));await refreshBalance()}catch(e:any){haptic('error');setMessage({id:task.id,text:e.message||t('taskFailed'),success:false})}setCompleting(null);setTimeout(()=>setMessage(null),2800)}const shown=useMemo(()=>filter==='all'?tasks:tasks.filter(x=>x.task_type===filter),[tasks,filter]);const filters=['all','social','daily','referral','video','special'];return <><style>{CSS}</style><div className="tk-root"><div className="tk-head"><div><div className="tk-kicker">{t('earnComplete')}</div><div className="tk-title">{t('taskBoard')}</div></div><div className="tk-head-icon"><ListChecks/></div></div><div className="tk-filters">{filters.map(f=><button key={f} className={`tk-filter ${filter===f?'active':''}`} onClick={()=>{haptic();setFilter(f)}}>{t(f)}</button>)}</div>{!loading&&<div className="tk-summary"><span>{t('tasksAvailable')}</span><b>{shown.length}</b></div>}{loading?<div className="tk-loading"><LoaderCircle/><div>{t('loadingTasks')}</div></div>:shown.length===0?<div className="tk-empty"><CheckCircle2/><div>{t('allTasksCompleted')}</div></div>:<div className="tk-list">{shown.map(task=>{const c=TYPE[task.task_type]||{key:'tasks',color:'#67e8f9',Icon:Globe2},Icon=c.Icon;return <div className="tk-card" key={task.id}><div className="tk-row"><div className="tk-icon" style={{color:c.color,background:`${c.color}14`,border:`1px solid ${c.color}28`}}><Icon/></div><div className="tk-body"><div className="tk-tag" style={{color:c.color}}>{t(c.key)}</div><div className="tk-name">{task.title}</div>{task.description&&<div className="tk-desc">{task.description}</div>}<div className="tk-reward" style={{color:c.color}}>+{task.reward_points.toLocaleString()} {t('points')}</div></div><button className="tk-go" disabled={completing===task.id} onClick={()=>void complete(task)} style={{background:c.color}}>{completing===task.id?<LoaderCircle/>:<ChevronRight/>}</button></div>{message?.id===task.id&&<div className={`tk-msg ${message.success?'ok':'err'}`}>{message.success?<CheckCircle2/>:<XCircle/>}{message.text}</div>}</div>})}</div>}</div></>}
